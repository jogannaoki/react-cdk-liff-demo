{
  "name": "line-demo",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/react-scripts",
      "**/react-scripts/**",
      "**/jest",
      "**/jest/**"
    ]
  },
  "scripts": {
    "test": "yarn run app:test",
    "build": "yarn run app:build",
    "app:start": "yarn workspace app start",
    "app:build": "yarn workspace app build",
    "app:test": "yarn workspace app test",
    "backend:build": "yarn workspace backend build",
    "backend:deploy": "yarn workspace backend deploy",
    "format": "yarn workspace app format && yarn workspace backend format"
  }
}
